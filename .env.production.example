# Production Environment Configuration
# Copy this to .env.production and update with your production values

# Database Configuration (Update with your production database details)
# For hosted databases like Neon, Railway, Supabase, etc.
# SSL will be automatically enabled for non-localhost connections
DB_HOST=your-production-db-host
DB_PORT=5432
DB_USERNAME=your-production-db-username
DB_PASSWORD=your-production-db-password
DB_DATABASE=your-production-db-name

# Database SSL Configuration (optional - overrides auto-detection)
# Set to 'true' to force SSL, 'false' to disable SSL
# If not set, SSL is enabled automatically for non-localhost connections in production
# DB_SSL=true

# Application Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration (Generate a secure secret for production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-very-long-and-secure-key

# Aging Configuration (in days)
AGING_ACTIVE_THRESHOLD=3
AGING_ATTENTION_THRESHOLD=7
AGING_OVERDUE_THRESHOLD=10
AGING_ESCALATION_THRESHOLD=10
AGING_DORMANT_THRESHOLD=30